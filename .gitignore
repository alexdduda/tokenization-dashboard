.venv/
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/

# data/treasuries.sqlite is deliberately NOT ignored. The daily GitHub Actions job
# commits it, which is what makes the snapshot history durable on free hosting and
# puts each day's data in git history. Do not add it here.

node_modules/
frontend/dist/
frontend/node_modules/
frontend/tsconfig.tsbuildinfo
